version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10
    working_directory: ClassWorkRepository/Sprint_3/project
    steps:
      - checkout
      - run: npm install --save-exact
      - run:
          name: Run tests
          command: npm test